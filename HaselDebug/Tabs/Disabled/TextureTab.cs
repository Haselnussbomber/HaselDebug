namespace HaselDebug.Tabs;
/*
public unsafe class TextureTab : DebugTab
{
    public override void Draw()
    {
        var texture = (Texture*)0x2B9AB4EF680;
        if (texture == null || texture->D3D11ShaderResourceView == null)
            return;

        var imageSize = new Vector2(texture->Width, texture->Height) / 2;
        ImGui.Image((IntPtr)texture->D3D11ShaderResourceView, imageSize);
    }
}
*/
